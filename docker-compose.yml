services:
  telethon-core:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telethon-core
    restart: unless-stopped
    user: "1000:1000"
    volumes:
      - .:/app/
    environment:
      - TZ=UTC
    env_file:
      - .env
